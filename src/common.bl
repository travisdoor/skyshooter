#load "sdl2/sdl2_image.bl"

load_surface :: fn (filepath: string) *SDL_Surface {
  surface := IMG_Load(filepath.ptr);
  if surface == null {
    print("Cannot load image.\n");
    abort();
  }

  return surface;
};